// Public API of the `complete-challenge` feature (CLAUDE.md §3.3). Ticking
// today's challenge off — the only write path for challenge completions.
export { useToggleChallenge } from './api/mutations';
